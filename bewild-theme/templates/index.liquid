{% comment %}
  BeWild Homepage Template
  Combines all sections in the proper order according to the brand strategy
{% endcomment %}

{% comment %} Hero Banner Section {% endcomment %}
{% section 'hero-banner' %}

{% comment %} Sustainability & Organic Farming Section {% endcomment %}
{% section 'sustainability-section' %}

{% comment %} Product Categories Section {% endcomment %}
{% section 'product-categories' %}

{% comment %} Farming Techniques Section {% endcomment %}
{% section 'farming-techniques' %}

{% comment %} Why Choose Us Section {% endcomment %}
{% section 'why-choose-us' %}

{% comment %} Team Experts Section {% endcomment %}
{% section 'experts-section' %}

{% comment %} Newsletter Signup Section {% endcomment %}
{% section 'newsletter-signup' %}

{% comment %}
  Additional sections can be added through the Shopify theme customizer
  - Testimonials
  - Blog posts
  - Featured products
  - etc.
{% endcomment %}

<script>
  // BeWild specific JavaScript for enhanced interactions
  document.addEventListener('DOMContentLoaded', function() {
    
    // Smooth scrolling for internal links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
    
    // Add scroll animations for section reveals
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('animate-in');
        }
      });
    }, observerOptions);
    
    // Observe all major sections
    document.querySelectorAll('[id*="section"], [id*="hero"], [id*="sustainability"], [id*="products"], [id*="farming"], [id*="why-choose"], [id*="experts"], [id*="newsletter"]').forEach(section => {
      observer.observe(section);
    });
    
    // Newsletter form enhancement
    const newsletterForm = document.querySelector('#newsletter-email');
    if (newsletterForm) {
      newsletterForm.addEventListener('focus', function() {
        this.parentElement.classList.add('focused');
      });
      
      newsletterForm.addEventListener('blur', function() {
        this.parentElement.classList.remove('focused');
      });
    }
    
  });
</script>

<style>
  /* Global animations and enhancements */
  [id*="section"],
  [id*="hero"],
  [id*="sustainability"],
  [id*="products"],
  [id*="farming"], 
  [id*="why-choose"],
  [id*="experts"],
  [id*="newsletter"] {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease-out;
  }
  
  [id*="section"].animate-in,
  [id*="hero"].animate-in,
  [id*="sustainability"].animate-in,
  [id*="products"].animate-in,
  [id*="farming"].animate-in,
  [id*="why-choose"].animate-in,
  [id*="experts"].animate-in,
  [id*="newsletter"].animate-in {
    opacity: 1;
    transform: translateY(0);
  }
  
  /* Remove animation for hero section - it should be visible immediately */
  [id*="hero"] {
    opacity: 1;
    transform: translateY(0);
  }
  
  /* Smooth transitions for all interactive elements */
  .btn,
  .category-cta,
  .newsletter-btn,
  .technique-card,
  .benefit-card,
  .expert-card,
  .category-card {
    transition: all 0.4s ease-in-out;
  }
  
  /* Enhanced focus states for accessibility */
  .btn:focus,
  .category-cta:focus,
  .newsletter-btn:focus,
  .newsletter-input:focus {
    outline: 3px solid #ffcf83;
    outline-offset: 2px;
  }
  
  /* Loading state for images */
  img {
    transition: opacity 0.3s ease;
  }
  
  img[loading="lazy"] {
    opacity: 0;
  }
  
  img[loading="lazy"].loaded {
    opacity: 1;
  }
  
  /* BeWild brand consistency helpers */
  .bewild-primary-color { color: #342e29; }
  .bewild-secondary-color { color: #86312b; }
  .bewild-accent-color { color: #ffcf83; }
  .bewild-green-color { color: #344736; }
  
  .bewild-primary-bg { background-color: #342e29; }
  .bewild-secondary-bg { background-color: #86312b; }
  .bewild-accent-bg { background-color: #ffcf83; }
  .bewild-green-bg { background-color: #344736; }
</style> 